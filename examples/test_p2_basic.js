// P2 最基础测试 - 一步步来

console.log("=== 基础测试 ===");

// 测试基本数组
var arr = [1, 2, 3];
console.log("基本数组:", arr);
console.log("长度:", arr.length);

// 测试嵌套数组
var nested = [
    [1, 2],
    [3, 4],
];
console.log("嵌套数组:", nested);
console.log("nested[0]:", nested[0]);
console.log("nested[1]:", nested[1]);

console.log("=== 测试完成 ===");
