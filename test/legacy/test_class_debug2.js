class Point {
    constructor(x, y) {
        console.log("x:", x, "y:", y);
        this.x = x;
        this.y = y;
    }
}

let p = new Point(1, 2);
console.log("p.x:", p.x);
console.log("p.y:", p.y);
